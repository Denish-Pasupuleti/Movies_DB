import React from 'react';
import SCHEMA from "./schema_image.png"

const Schema = () => {

	return (
		<>
			<div style={{marginLeft: "auto", marginRight: "auto"}}>
				<img src={SCHEMA} alt='SCHEMA' width={'30%'} ></img>
			</div>
		</>
	);
};

export default Schema;
